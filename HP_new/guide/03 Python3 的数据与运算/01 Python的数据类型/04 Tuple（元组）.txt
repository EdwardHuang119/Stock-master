'''
    Tuple（元组）

    Tuple（元组）是一种有序列表。Tuple和List非常类似，但是Tuple一旦
初始化就不能修改。元组使用小括号“()”来包含元组元素，元组元素也用
逗号“,”来分隔元素。元组的索引号也是从0开始，例如：
'''

stocks=('000001','600080','300005') 
print(type(stocks))
print(stocks)
